name: carrot_market_ui
description: A new Flutter project.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  font_awesome_flutter: ^9.2.0 # 여러 가지 아이콘
  google_fonts: ^2.1.0 # 여러 가지 글꼴 스타일
  intl: ^0.17.0 # DateFormat, NumberFormat 등 기능

dev_dependencies:
  flutter_lints: ^1.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
